# =============================================================================
# This file defines properties used by build-impl.xml, which is the core of
# the applications build process. You can customize this file to control the
# various options used in the testing build.
#
# IMPORTANT - Sencha Cmd will merge your changes with its own during upgrades.
# To avoid potential merge conflicts avoid making large, sweeping changes to
# this file.
# =============================================================================

# ===========================================
# various js / compiler properties
# ===========================================

# These properties can be modified to change general build options
# such as excluding files from the set.  The format expects newlines
# for each argument, for example:
#            
#   build.operations=\
#       exclude\n \
#       -namespace=Ext\n

build.operations.testing=

build.operations=${build.operations.testing}

# Specify preprocessor options

build.options.testing.debug=debug:false
build.options.testing.logger=logger:no
build.options.testing.minVersion=minVersion:3
build.options.testing.product=product:touch

build.options.debug=${build.options.testing.debug}
build.options.logger=${build.options.testing.logger}
build.options.minVersion=${build.options.testing.minVersion}
build.options.product=${build.options.testing.product}
